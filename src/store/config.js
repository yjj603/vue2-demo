import location from "@/store/config/location";
import datacenter from "@/store/config/datacenter";
import cabinet from "@/store/config/cabinet";
import server from "@/store/config/server";
export default {
    state:{
        location,
        datacenter,
        cabinet,
        server,
    }
}